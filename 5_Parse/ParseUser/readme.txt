Information:
- the ParseUser.enableAutomaticUser(); in the StarterApplication creates automatic users if there is no login or so
	-> later the username and password for the user can be updated

content:
- Sign up a ParseUser (with username and password):
	<Sign up a user>

- Log in a user:
	<Log in a User>

- Log in ParseUser without Username (anonymous):
	<Log in a User anonymous>

- Check if a User is logged in
	<check if user is logged in>

- Log out a User:
	<Log out a user>

- get a list of all Users:
	<get a list of all users>